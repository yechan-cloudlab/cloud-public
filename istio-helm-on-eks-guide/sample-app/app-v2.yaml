apiVersion: apps/v1
kind: Deployment
metadata:
  name: httpbin-v2
  namespace: istio-sample
spec:
  replicas: 2
  selector:
    matchLabels:
      app: httpbin
      version: v2
  template:
    metadata:
      labels:
        app: httpbin
        version: v2
    spec:
      containers:
        - name: httpbin
          image: docker.io/kong/httpbin:0.2.2
          ports:
            - containerPort: 80
