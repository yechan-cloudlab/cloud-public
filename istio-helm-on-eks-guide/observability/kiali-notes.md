# Kiali notes

Kiali is useful for visually checking service-to-service traffic.

Check:

- whether traffic reaches the expected workload;
- whether a VirtualService route is being applied;
- whether mTLS is enabled;
- whether errors are concentrated around a specific edge.
